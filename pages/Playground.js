import React from "react";

export default props => {
    return (
        <div>
            <h1>Playground</h1>
            <button className="btn btn-primary">generate</button>
        </div>
    );
};
